# Neon / Vercel Postgres (from Vercel dashboard → Storage → Postgres)
DATABASE_URL=
POSTGRES_URL=

# Gemini API (server-side only — never expose to client)
GEMINI_API_KEY=

# Auth (generate with: openssl rand -base64 32)
AUTH_SECRET=
